# Copyright 2025 Canonical Ltd.
# See LICENSE file for licensing details.
name: Check pull request

on:
  pull_request:
    types:
      - opened
      - edited
    branches:
      - main

jobs:
  check-pr:
    name: Check pull request
    # TODO update after https://github.com/canonical/data-platform-workflows/pull/294 merged
    uses: canonical/data-platform-workflows/.github/workflows/check_python_package_pr.yaml@release-python-package
